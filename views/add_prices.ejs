<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8"> 
    <title>Add Products</title>
</head>
<body>

    <form>
        <div>
            <label for="prices">Add Prices:</label>
            <input type="file" name="prices" id="prices">
        </div>
        <button type="submit">Submit</button>

    </form>

    <br></br>
    <div id="delete_all">
        <label for="delete">Delete All :</label>
        <button id="delete_btn" onclick="Sure()">Delete</button>
    </div>
<script>
    const form = document.querySelector('form');
    form.addEventListener('submit', (e) => {
    
    e.preventDefault();


/////////////////////////////////////////////////////
    const Price = document.getElementById("prices");
    const priceData = new FormData();
  
    for(let i =0; i < Price.files.length; i++) {
        priceData.append("Price", Price.files[i]);
    }
    console.log(...priceData)
 ////////////////////////////////////////////////
 
////////////////////////////////////////////

    fetch('/add_product_prices', {
        method: 'POST',
        body: priceData, 
    })
    .then(res => res.json())
    .then(data => console.log(data))


})

function Sure(){
    const del = document.getElementById("delete_all")
    let sure = `<label for="delete">Are you sure:</label>
                <button id="Yes" onclick="delete_all()">Yes</button>
                <button id="Cansel" onclick="reload()">Cancel</button>
                `

    del.innerHTML = `${sure}`
}

function reload(){
    location.reload();
}

function delete_all(){
    fetch('/delete_products', {
        method: 'POST',
    }).then(alert("All products deleted"))
    .then(location.reload())
}

</script>

</body>
</html>